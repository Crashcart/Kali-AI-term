# Kali Hacker Bot Configuration Template
# Copy this to .env and customize for your environment

# ============================================
# Application Settings
# ============================================

# Node.js environment (production or development)
NODE_ENV=production

# Express server port
PORT=3000

# Bind address (0.0.0.0 for all interfaces, localhost for local only)
BIND_HOST=0.0.0.0

# ============================================
# Ollama Configuration
# ============================================

# Primary Ollama API endpoint
# Use http://host.docker.internal:11434 for Docker Desktop
# Use http://ollama:11434 if Ollama is in another container
# Use http://192.168.1.100:11434 for remote Ollama instance
OLLAMA_URL=http://host.docker.internal:11434

# Default LLM model to use.
# dolphin-mixtral requires ~24.8 GiB RAM and will OOM on systems with less.
# Lightweight alternatives that work on most systems:
#   phi3:mini      (~2.2 GiB)  — recommended default
#   tinyllama      (~637 MiB)  — ultra-lightweight
#   llama3.2:3b    (~2 GiB)    — good quality/size balance
OLLAMA_MODEL=phi3:mini

# Freeze LLM processing (set true to disable all model calls without crashing)
# Automatically managed by .github/workflows/unfreeze-llm.yml (runs every 3 days)
LLM_FROZEN=false

# Multiple Ollama instances (optional, comma-separated)
# When set, OLLAMA_URLS takes precedence over OLLAMA_URL.
# The first entry is always the primary instance.
# Example: OLLAMA_URLS=http://localhost:11434,http://192.168.1.50:11434
# OLLAMA_URLS=

# Enable automatic network scanning for Ollama instances (off by default)
# When true the "Scan Network" button in Settings is unlocked by default.
# Users can still toggle this per-session from the UI regardless of this value.
OLLAMA_NETWORK_SCAN=false

# ============================================
# Google Gemini Configuration (optional)
# ============================================

# Google Gemini API key for hybrid cloud/local LLM reasoning.
# When set, Gemini is used as the primary provider for complex reasoning tasks
# with Ollama as the fallback.  Leave blank to use Ollama only.
# Obtain a key at https://aistudio.google.com/apikey
# GEMINI_API_KEY=your-gemini-api-key-here

# Default Gemini model (optional, defaults to gemini-1.5-flash)
# Other options: gemini-1.5-pro, gemini-2.0-flash-lite
# GEMINI_MODEL=gemini-1.5-flash

# ============================================
# Docker & Container Settings
# ============================================

# Kali Linux container name (must match docker-compose.yml)
KALI_CONTAINER=kali-ai-term-kali

# ============================================
# Security
# ============================================

# Admin password for web UI authentication
# Generate secure password: node -e "console.log(require('crypto').randomBytes(8).toString('hex'))"
ADMIN_PASSWORD=kalibot

# Session authentication secret (must be unique)
# Generate: node -e "console.log(require('crypto').randomUUID())"
AUTH_SECRET=your-random-uuid-here-replace-this

# ============================================
# Logging
# ============================================

# Log level: debug, info (default), warn, error
# debug   - Very verbose output for troubleshooting
# info    - Standard logging (recommended)
# warn    - Warnings and errors only
# error   - Errors only
LOG_LEVEL=info

# ============================================
# Database
# ============================================

# Database file location (relative to app root)
DB_PATH=./data/kali.db

# ============================================
# Optional Settings
# ============================================

# Installation diagnostic file (auto-generated)
# DIAGNOSTIC_FILE=./install.diagnostic

# Custom data directory for persistence
# DATA_DIR=./data
